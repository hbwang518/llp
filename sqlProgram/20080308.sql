select * from aspectCode
select id,aspect,aspectCode,  from m_grid

--update m_grid set aspectCode = a.code from m_grid, aspectCode a where m_grid.aspect >= a.minAspect and m_grid.aspect < a.maxAspect

SELECT *
FROM M_Plant where cnfamily like '%É¯²Ý¿Æ%'
WHERE (latinGenus IS NULL)

SELECT *
FROM M_Plant
WHERE (latinName LIKE '%Gentiana%')
